# {{Your Name}} — LLM Wiki · Log

Append-only, chronological record of every ingest, query, and lint pass. Newest at the bottom. Each entry starts with `## [YYYY-MM-DD] <op> | <title>` so it stays greppable: `grep "^## \[" log.md | tail -5`.

## [{{YYYY-MM-DD}}] init | Wiki scaffolded
Created the wiki per Karpathy's LLM Wiki pattern: schema (`CLAUDE.md`), master `index.md`, this log, and `README.md`. Set up the domain folders (each with an `index.md` and a `raw/` folder). No sources ingested yet.
